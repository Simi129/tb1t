import { Injectable } from '@nestjs/common';
import { Context } from 'telegraf';

@Injectable()
export class HelpCommand {
  async execute(ctx: Context) {
    const helpText = 
      `📖 **Справка**\n\n` +
      `**Основные команды:**\n` +
      `/start - Начать работу\n` +
      `/help - Показать эту справку\n` +
      `/profile - Ваш профиль\n\n` +
      `**Мониторинг:**\n` +
      `/ping - Быстрая проверка задержки\n` +
      `/status - Детальная диагностика\n\n` +
      `**AI возможности:**\n` +
      `🍌 /imagine <описание> - Сгенерировать изображение\n` +
      `🖼️ Отправь фото - Анализ изображения\n` +
      `🖼️ Фото + подпись - Редактирование (Nano Banana)\n` +
      `🎤 Голосовое - Расшифровка\n` +
      `🎵 Аудио - Анализ\n` +
      `🎬 Видео - Описание содержания\n\n` +
      `**О проекте:**\n` +
      `🔥 ProspectTrade VIP\n` +
      `⚡ Vercel + Supabase + Gemini AI`;

    await ctx.reply(helpText, { parse_mode: 'Markdown' });
  }
}