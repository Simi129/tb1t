import { Injectable, Logger } from '@nestjs/common';
import { Context } from 'telegraf';
import { mainKeyboard } from '../keyboard.config';

@Injectable()
export class HelpCommand {
  private readonly logger = new Logger(HelpCommand.name);

  async execute(ctx: Context) {
    const helpText = 
      `📖 *Справка по боту*\n\n` +
      `*🎹 Главное меню:*\n` +
      `👤 *Профиль* \\- ваши данные и статистика\n` +
      `🤖 *Gemini AI* \\- чат и анализ контента\n` +
      `🖼 *Генерация изображений* \\- создание картинок\n` +
      `🎬 *Видео с ИИ* \\- анализ видео контента\n` +
      `🎙 *Аудио с ИИ* \\- транскрибация и анализ\n\n` +
      `*⚡️ Основные команды:*\n` +
      `\`/start\` \\- Запустить бота\n` +
      `\`/help\` \\- Эта справка\n` +
      `\`/profile\` \\- Ваш профиль\n\n` +
      `*🍌 AI возможности:*\n` +
      `\`/imagine [описание]\` \\- Генерация изображения\n` +
      `🖼️ Отправь фото \\- Анализ изображения\n` +
      `🎤 Голосовое \\- Расшифровка\n` +
      `🎵 Аудио \\- Анализ содержания\n` +
      `🎬 Видео \\- Описание контента\n\n` +
      `*📊 Мониторинг:*\n` +
      `\`/ping\` \\- Проверка задержки\n` +
      `\`/status\` \\- Диагностика системы\n\n` +
      `*🎯 Примеры использования:*\n` +
      `• *Чат:* Gemini AI → 💬 Чат с Gemini\n` +
      `• *Анализ фото:* Gemini AI → 🔍 Анализ изображения\n` +
      `• *Создать картинку:* \`/imagine кот в космосе\`\n` +
      `• *Расшифровка:* Аудио с ИИ → 📝 Транскрибация\n\n` +
      `*О проекте:*\n` +
      `🔥 ProspectTrade VIP\n` +
      `⚡ Vercel \\+ Supabase \\+ Gemini AI\n\n` +
      `Используйте кнопки меню для навигации\\! 👇`;

    await ctx.reply(helpText, {
      parse_mode: 'MarkdownV2',
      ...mainKeyboard
    });

    this.logger.log(`Help command executed for user ${ctx.from?.id}`);
  }
}